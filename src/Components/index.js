import * as global from './Global';
import * as layouts from './Layouts';
import * as pages from './Pages';
import * as widgets from './Widgets';

export default {
  ...global, ...layouts, ...widgets, ...pages
};
