// import actions from './actions';

const initState = {
  ProductList: []
};

export default (state = initState, action) => {
  switch (action.type) {
    default:
      return {
        ...state
      };
  }
};
